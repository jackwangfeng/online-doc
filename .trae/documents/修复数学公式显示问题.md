## 问题分析
数学公式显示不正确，显示了完整的KaTeX HTML结构（包括MathML和katex-html），而不是简洁的渲染结果。

## 解决方案
采用更直接的方法，移除复杂的MathFormula扩展，直接在Editor.tsx中处理数学公式的插入和渲染。

## 实施步骤

### 1. 修改Editor.tsx
- 移除 `import { MathFormula } from './extensions/MathFormula'`
- 从extensions数组中移除 `MathFormula`
- 重新实现 `insertMathFormula` 函数，使用更简单的方法

### 2. 更新CSS样式
- 在Editor.css中添加数学公式样式
- 隐藏MathML部分（`display: none`）
- 确保katex-html部分正确显示

### 3. 更新PaginationPreview.css
- 添加分页预览中的数学公式样式

### 4. 删除MathFormula扩展文件
- 删除 `/home/jeffwang/work/temp/doc_online/client/src/extensions/MathFormula.ts`

## 预期结果
- 数学公式正确渲染显示
- 不显示多余的HTML结构
- 在编辑模式和分页预览模式都能正常工作

请确认这个计划后，我将开始执行具体的修改。