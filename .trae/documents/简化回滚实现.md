按照用户确认的方案实现回滚功能：

**方案**：

1. 直接从数据库获取目标版本的 update\_data
2. 将该 update\_data 作为新的更新追加到 yjs\_updates 表末尾
3. 不删除任何历史，所有版本都保留
4. 清除缓存让客户端重新同步

**修改文件**：

* `/home/jeffwang/work/temp/doc_online/server/yjs-persistence.js` - 简化 rollbackToVersion 方法

* `/home/jeffwang/work/temp/doc_online/server/index.js` - 确保 API 正确处理（可能需要调整）

* `/home/jeffwang/work/temp/doc_online/client/src/Editor.tsx` - 更新提示信息

**核心代码逻辑**：

```javascript
// 1. 获取目标版本的更新数据
const result = await db.query(
  'SELECT update_data FROM yjs_updates WHERE id = $1 AND room_name = $2',
  [versionId, roomName]
)

// 2. 直接追加到末尾
await db.query(
  'INSERT INTO yjs_updates (room_name, update_data) VALUES ($1, $2)',
  [roomName, result.rows[0].update_data]
)

// 3. 清除缓存
this.clearDocument
```

